enum Visibility { 
    PUBLIC 
    PRIVATE 
}

enum MediaType { 
    IMAGE 
    VIDEO 
    AUDIO 
    EMBED
}

enum MediaStatus {
    NEW
    PENDING_UPLOAD
    UPLOADING
    UPLOADED
    FAILED_UPLOAD
    VALIDATING
    REJECTED
    READY
    PROCESSING
    READY_WITH_VARIANTS
    FAILED_PROCESSING
    DELETING
    DELETED
    // Legacy states for backward compatibility
    PENDING
    FAILED
}

enum LikeAction { 
    LIKE 
    DISLIKE 
}

enum MatchState { 
    ACTIVE 
    BLOCKED 
    CLOSED 
}

enum Gender { 
    UNSPECIFIED 
    MALE 
    FEMALE 
    NONBINARY 
    OTHER 
}

enum DatingIntent {
     UNSPECIFIED 
     FRIENDS 
     CASUAL 
     LONG_TERM 
     MARRIAGE 
}

enum ReportReason {
     SPAM 
     HARASSMENT 
     IMPERSONATION 
     NUDITY 
     HATE 
     OTHER 
}

enum ProfileAccessStatus {
     PENDING
     GRANTED
     DENIED
     REVOKED
     CANCELED
}

enum ProfileAccessSource {
     PROFILE
     INBOX
     SYSTEM
}

enum UserRole {
     USER
     ADMIN
     SUPER_ADMIN
}

enum JobRunStatus {
     QUEUED
     RUNNING
     SUCCESS
     FAILED
     CANCELLED
}

enum JobTrigger {
     CRON
     EVENT
     MANUAL
}

enum CompatibilityStatus {
     READY
     INSUFFICIENT_DATA
}

enum FeedItemType {
     POST
     SUGGESTION
}

enum SampleCategory {
     BEST
     MIDDLE
     WORST
}

enum GeoPrecision {
     EXACT
     CITY
     STATE
     UNKNOWN
}

enum LocationAccuracy {
     FRESH
     STALE
}
